<odoo>
    <record id="setting_view" model="ir.ui.view">
        <field name="name">setting.view.form</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="base_setup.res_config_settings_view_form" />
        <field name="arch" type="xml">
            <xpath expr="//div/block" position="inside">
                <h2>My settings</h2>
                <setting id="barcode_generation_setting_id">
                    <label for="enable_barcode" class="mx-2">Enable barcode generation</label>
                    <field name="enable_barcode" />
                    <label for="enable_creation" class="mx-2">Enable Creation</label>
                    <field name="enable_creation" />
                </setting>
            </xpath>
        </field>
    </record>

    <record id="settings_action_id" model="ir.actions.act_window">
        <field name="name">Settings</field>
        <field name="res_model">res.config.settings</field>
        <field name="view_mode">form</field>
        <field name="target">current</field>
    </record>
</odoo>